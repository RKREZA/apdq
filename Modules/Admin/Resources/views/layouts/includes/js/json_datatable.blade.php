
    language: {
        processing:     "{{ __('admin::datatable.processing') }}",
        search:         "{{ __('admin::datatable.search') }}&nbsp;:",
        lengthMenu:    "{{ __('admin::datatable.show') }} _MENU_ {{ __('admin::datatable.entries') }}",
        info:           "{{ __('admin::datatable.showing') }} _START_ {{ __('admin::datatable.to') }} _END_ {{ __('admin::datatable.of') }} _TOTAL_ {{ __('admin::datatable.entries') }}",
        infoEmpty:      "{{ __('admin::datatable.showing') }} 0 {{ __('admin::datatable.to') }} 0 {{ __('admin::datatable.of') }} 0 {{ __('admin::datatable.entries') }}",
        infoFiltered:   "({{ __('admin::datatable.filtered_from') }} _MAX_ {{ __('admin::datatable.total_entries') }})",
        infoPostFix:    "",
        loadingRecords: "{{ __('admin::datatable.loading') }}",
        zeroRecords:    "{{ __('admin::datatable.no_matching_record_found') }}",
        emptyTable:     "{{ __('admin::datatable.no_data_available_in_table') }}",
        paginate: {
            first:      "{{ __('admin::datatable.first') }}",
            previous:   "{{ __('admin::datatable.previous') }}",
            next:       "{{ __('admin::datatable.next') }}",
            last:       "{{ __('admin::datatable.last') }}"
        },
    },
